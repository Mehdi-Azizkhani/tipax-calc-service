namespace Domain.Common.Enums;

public static class Constants
{
    public enum SrviceType
    {
        Service = 1,
        Microservice = 2,
    }

    public enum LocationLimitType
    {
        //none = 10,
    }

    public enum ParcelAttributeLimitType
    {
        //none = 20,
    }

    public enum OrderChannelLimitType
    {
        //none = 30,
    }
}
